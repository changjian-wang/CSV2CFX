<ui:FluentWindow x:Class="Flex.Csv2Cfx.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="系统登录" 
        Height="400" 
        Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">
            <!-- Logo/标题 -->
            <TextBlock Text="WPF消息系统" 
                      FontSize="24" 
                      FontWeight="Bold" 
                      HorizontalAlignment="Center"
                      Margin="0,0,0,30"/>

            <!-- 登录表单 -->
            <StackPanel>
                <ui:TextBox PlaceholderText="用户名"
                        Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,0,0,15"/>

                <ui:PasswordBox PlaceholderText="密码"
                           x:Name="PasswordBoxControl"
                           PasswordChanged="PasswordBox_PasswordChanged"
                           Margin="0,0,0,15"/>

                <!-- 错误信息 -->
                <Border Background="#FFE7E7" 
                       BorderBrush="#FF6B6B" 
                       BorderThickness="1" 
                       CornerRadius="4"
                       Padding="10,8"
                       Margin="0,0,0,15"
                       Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="{Binding ErrorMessage}" 
                              Foreground="#D13438"
                              TextWrapping="Wrap"/>
                </Border>

                <!-- 登录按钮 -->
                <ui:Button Content="登录" 
                       Command="{Binding LoginCommand}"
                       CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                       Appearance="Primary"
                       HorizontalAlignment="Stretch"
                       Height="35"/>
            </StackPanel>

            <!-- 测试账号提示 -->
            <TextBlock Text="测试账号: admin/admin123 或 user/user123" 
                      FontSize="11" 
                      Foreground="Gray"
                      HorizontalAlignment="Center"
                      Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>